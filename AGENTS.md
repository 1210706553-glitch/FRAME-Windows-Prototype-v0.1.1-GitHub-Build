# 张诗语の米奇妙妙工具：Codex 项目规则

## 开始工作前

- 先完整阅读 `docs/CODEX-HANDOFF.md`、`tasks/plan.md` 和 `tasks/todo.md`。
- 先检查仓库状态、当前版本和现有改动；不要覆盖用户尚未提交的修改。
- 先复现或核对问题，再实现；不要把界面存在误报为功能可用。

## 产品边界

- 这是 Windows 本地视频创作执行工具，核心是每日任务清单、可视化剩余进度、提醒和专注约束。
- 一次只维护一个当前项目；每个人独立使用，不做项目共享或云同步。
- 数据默认只保存在本机。升级必须保留 `com.sunday.frame` 标识和既有 localStorage 数据。
- 保持软件精简。不要恢复已删除的视频导入、播放器、素材时间线、波形图、多项目库或 Premiere XML。
- AI 只参与“素材梳理、粗剪、大纲”三个阶段；后续阶段只负责排任务和督促。
- AI 接口优先适配 DeepSeek，不默认依赖昂贵的 OpenAI API。
- 不要伪装未实现功能。网站限制、AI分析等未接通时必须明确显示状态。

## 设计原则

- 风格：专业底座加少量创作者个性；深色、清晰，不沉闷也不花哨。
- 首页始终突出唯一下一步、今天还差多少、项目还差多少、预计何时完成。
- 降低用户操作成本，优先选择器、按钮和合理默认值，少让用户手动输入技术字段。
- 新功能优先放进现有流程，除非确有必要，不新增导航页。

## 工程要求

- 技术栈：React 19、TypeScript、Vite、Tauri 2、Rust。
- 修改前阅读相关实现和测试，保持现有模式。
- 前端变更必须运行：`npm run lint`、`npm run test`、`npm run build`。
- Rust 变更必须运行：`cargo test --manifest-path src-tauri/Cargo.toml`；若本机没有 Rust，明确标为待 GitHub Actions 验证。
- Windows 安装包由 `.github/workflows/windows-build.yml` 构建，只上传 NSIS EXE。
- 不随意增加生产依赖；确需增加时先说明原因和替代方案。
- 不使用 hosts、防火墙或注册表实现临时程序限制。程序退出后不得遗留系统修改。
- 强制关闭程序前必须保护系统关键进程和本软件自身，并提示未保存数据风险。

## 交付标准

- 结论先行，区分已验证、合理推测和待真机验证。
- 完成功能后更新 `README.md`、`tasks/plan.md`、`tasks/todo.md` 和版本号。
- 不把“能编译”当成“真机可用”；Windows 原生功能必须安排安全的真实设备测试。
