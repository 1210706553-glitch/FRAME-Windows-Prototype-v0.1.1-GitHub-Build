# Optional local binaries

Future packaged builds can place the following Windows binaries here:

- `ffmpeg.exe`
- `ffprobe.exe`
- `frame-transcriber.exe`

The current prototype first checks commands available on the system `PATH`. Binary redistribution and third-party license notices must be reviewed before bundling them in a release.
